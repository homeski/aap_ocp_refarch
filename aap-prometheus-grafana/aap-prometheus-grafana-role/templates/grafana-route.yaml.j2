apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grafana
  namespace: {{ prometheus_namespace }}
spec:
  host: grafana-prometheus-operator.apps.sno.escwq.xyz
  port:
    targetPort: grafana
  to:
    kind: Service
    name: grafana-a-service
    weight: 100
  wildcardPolicy: None
